[pytest]
# Confine collection to tests/. Without this, a bare `pytest` from the repo
# root also collects scripts/test_*.py -- diagnostic scripts that make real
# API calls at import time, so simply collecting them spends money and fails
# without keys. They are tools, not tests.
testpaths = tests
